---
# yaml-language-server: $schema=https://json.schemastore.org/helmfile
# Legacy entrypoint for task-based bootstrap. Prefer `just bootstrap` or
# `helmfile -f bootstrap/helmfile/apps.yaml sync`.
bases:
  - helmfile/apps.yaml
