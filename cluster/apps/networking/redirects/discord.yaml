---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    nginx.ingress.kubernetes.io/permanent-redirect: "https://discord.com/invite/99kHgMHXA6"
  name: redirect
  namespace: networking
spec:
  rules:
  - host: discord.blackelement.studio
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ingress-nginx-defaultbackend
            port:
              number: 80
  tls:
  - hosts:
    - discord.blackelement.studio
    secretName: discord-tls
