---
apiVersion: v1
kind: Service
metadata:
  name: gitea
  namespace: external
  annotations: {}
  #  traefik.ingress.kubernetes.io/service.serversscheme: https
spec:
  type: ExternalName
  externalName: "${NAS_ADDR}"
  ports:
    - port: 3010
