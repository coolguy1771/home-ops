---
apiVersion: v1
kind: Service
metadata:
  name: gitea
  namespace: external
  annotations: {}
  #  nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
spec:
  type: ExternalName
  externalName: "osiris.${SECRET_PRIVATE_DOMAIN}"
  ports:
    - port: 9080
