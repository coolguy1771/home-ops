---
apiVersion: v1
kind: Namespace
metadata:
  name: networking
  labels:
    istio.io/dataplane-mode: ambient
    istio-injection: enabled
    kustomize.toolkit.fluxcd.io/prune: disabled
  annotations:
    istio-injection: enabled
