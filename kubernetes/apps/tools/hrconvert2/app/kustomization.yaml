---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: tools
resources:
  - ./helmrelease.yaml
configMapGenerator:
  - name: hrconvert2-config-configmap
    files:
      - config.php=./config/config.php
configurations:
  - ./patches/kustomizeconfig.yaml
