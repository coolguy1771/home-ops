---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/tuppr.home-operations.com/talosupgrade_v1alpha1.json
apiVersion: tuppr.home-operations.com/v1alpha1
kind: TalosUpgrade
metadata:
  name: talos
spec:
  talos:
    # renovate: datasource=github-releases depName=siderolabs/talos
    version: v1.13.9
  policy:
    rebootMode: powercycle
  healthChecks:
    - description: "All Snapshots idle (no mover Job reconciling)"
      apiVersion: kopiur.home-operations.com/v1alpha1
      kind: Snapshot
      expr: |-
        status.conditions.filter(c, c.type == "Reconciling").all(c, c.status == "False")
    - description: "CephCluster HEALTH_OK"
      apiVersion: ceph.rook.io/v1
      kind: CephCluster
      expr: |-
        status.ceph.health in ['HEALTH_OK']
