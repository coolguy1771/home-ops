---
# yaml-language-server: $schema=https://kubernetes-schemas.witl.xyz/provider_v1.json
apiVersion: notification.toolkit.fluxcd.io/v1
kind: Provider
metadata:
  name: github
  namespace: flux-system
spec:
  type: github
  address: https://github.com/coolguy1771/home-ops
  interval: 10m
  secretRef:
    name: github-token-secret
---
# yaml-language-server: $schema=https://kubernetes-schemas.witl.xyz/alert_v1.json
apiVersion: notification.toolkit.fluxcd.io/v1beta2
kind: Alert
metadata:
  name: github
  namespace: flux-system
spec:
  providerRef:
    name: github
  eventSeverity: info
  eventSources:
    - kind: Kustomization
      name: "*"
    - kind: HelmRelease
      name: "*"
