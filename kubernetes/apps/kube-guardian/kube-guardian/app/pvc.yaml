---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: guardian-db
  namespace: xentra
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: ceph-block
  resources:
    requests:
      storage: 10Gi
